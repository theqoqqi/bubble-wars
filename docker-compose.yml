services:
  bubble-wars:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bubble-wars
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - NODE_ENV=production
